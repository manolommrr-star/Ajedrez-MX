/**
 * Configuración general de la aplicación Vue.
 * (Equivalente a js/config.js del prototipo vanilla.)
 */
export const AppConfig = {
  nombreApp: 'AjedrezMX',
  fuenteDatos: 'mock', // 'mock' | 'supabase' (fase 2)

  /** Días que definen la sección "Torneos cercanos". */
  rangoCercanosDias: 45
};